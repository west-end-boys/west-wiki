{
  "name": "auto-resume-harness",
  "version": "1.0.0",
  "description": "Auto-resume harness for autonomous phase execution",
  "main": "auto-resume-harness.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": ">=0.40.0",
    "@anthropic-ai/claude-agent-sdk": "*"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
